{
  "createOrder": {
    "type": "object",
    "required": ["user_id", "total_amount"],
    "properties": {
      "user_id": {
        "type": "integer"
      },
      "address_id": {
        "type": "integer"
      },
      "cart_id": {
        "type": "integer"
      },
      "total_amount": {
        "type": "number"
      },
      "status": {
        "type": "string",
        "enum": ["pending", "paid", "shipped", "delivered", "cancelled"]
      },
      "applied_discount_id": {
        "type": "integer"
      }
    }
  },
  "updateOrder": {
    "type": "object",
    "properties": {
      "address_id": {
        "type": "integer"
      },
      "cart_id": {
        "type": "integer"
      },
      "total_amount": {
        "type": "number"
      },
      "status": {
        "type": "string",
        "enum": ["pending", "paid", "shipped", "delivered", "cancelled"]
      },
      "applied_discount_id": {
        "type": "integer"
      }
    }
  },
  "getOrderParams": {
    "type": "object",
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[0-9]+$"
      }
    }
  },
  "deleteOrderParams": {
    "type": "object",
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[0-9]+$"
      }
    }
  },
  "getOrdersQuery": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "pattern": "^[0-9]+$"
      },
      "status": {
        "type": "string",
        "enum": ["pending", "paid", "shipped", "delivered", "cancelled"]
      }
    }
  }
}

