{
  "createDiscountRule": {
    "type": "object",
    "required": ["code", "type", "value", "valid_from", "valid_to"],
    "properties": {
      "code": {
        "type": "string",
        "minLength": 1,
        "maxLength": 50
      },
      "type": {
        "type": "string",
        "enum": ["percent", "fixed"]
      },
      "value": {
        "type": "number"
      },
      "valid_from": {
        "type": "string",
        "format": "date-time"
      },
      "valid_to": {
        "type": "string",
        "format": "date-time"
      },
      "is_active": {
        "type": "boolean"
      }
    }
  },
  "updateDiscountRule": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "minLength": 1,
        "maxLength": 50
      },
      "type": {
        "type": "string",
        "enum": ["percent", "fixed"]
      },
      "value": {
        "type": "number"
      },
      "valid_from": {
        "type": "string",
        "format": "date-time"
      },
      "valid_to": {
        "type": "string",
        "format": "date-time"
      },
      "is_active": {
        "type": "boolean"
      }
    }
  },
  "getDiscountRuleParams": {
    "type": "object",
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[0-9]+$"
      }
    }
  },
  "deleteDiscountRuleParams": {
    "type": "object",
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[0-9]+$"
      }
    }
  },
  "getDiscountRulesQuery": {
    "type": "object",
    "properties": {
      "is_active": {
        "type": "string",
        "enum": ["true", "false"]
      },
      "type": {
        "type": "string",
        "enum": ["percent", "fixed"]
      }
    }
  }
}

